[pytest]
testpaths = tests
markers =
    unit: no live services required; runs on macOS
    integration: requires OPNsense box (OPNSENSE_HOST env var)
    packaging: requires OPNsense build tools on the target box
    slow: takes more than 5 seconds
filterwarnings =
    ignore::DeprecationWarning
