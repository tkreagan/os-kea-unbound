# "kea-ub" is chosen to avoid the substring "unbound": the OPNsense core resolver
# filter program("unbound") uses unanchored substring matching and would capture
# any tag containing "unbound", routing our log lines to the resolver log instead.
filter f_local_keaunbound {
    program("kea-ub");
};
